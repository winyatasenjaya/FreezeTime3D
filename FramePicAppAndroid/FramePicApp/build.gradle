import groovy.json.JsonSlurper
apply plugin: 'com.android.application'

android {
    compileSdkVersion 21
    buildToolsVersion "21.1.1"

    defaultConfig {
        minSdkVersion 15
        targetSdkVersion 21
        versionCode 1
        versionName "1.0"

        def jsonFile = new File("../Server/socketMessages.json")
        def jsonMessages = new JsonSlurper().parseText(jsonFile.text)

        for (i in 0..9) {
            buildConfigField("String", "MyVar", jsonMessages.masterMessages.register)
            println(jsonMessages.masterMessages.register)
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.koushikdutta.async:androidasync:1.1.6'
}
